{
  "name": "pescador",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "lint": "eslint 'packages/**/*.{js,ts,tsx}'",
    "lint:fix": "eslint 'packages/**/*.{js,ts,tsx}' --fix",
    "build:graph": "npm run build --workspace=@pescador/graph",
    "deploy:all": "npx ts-node scripts/deploy/deployAll.ts",
    "deploy:service": "ts-node scripts/deploy/deployService.ts"
  },
  "author": "Cory Sorel",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@eslint/js": "^9.18.0",
    "@types/serverless": "^3.12.26",
    "serverless": "^4.4.18",
    "ts-loader": "^9.5.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.20.0"
  },
  "dependencies": {
    "@aws-sdk/client-lambda": "^3.738.0"
  }
}
